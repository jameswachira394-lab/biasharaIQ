apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: biasharaiq

resources:
  - namespace.yaml
  - secrets.yaml
  - persistent-volume-claims.yaml
  - postgresql.yaml
  - redis.yaml
  - backend.yaml
  - frontend.yaml
